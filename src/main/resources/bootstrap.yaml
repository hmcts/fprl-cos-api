spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/div
      aliases:
        fprl.uk-gov-notify-api-key: UK_GOV_NOTIFY_API_KEY
        fprl-idam-client-secret: idam.client.secret
